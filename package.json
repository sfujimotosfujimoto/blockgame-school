{
  "name": "blockgame",
  "version": "1.0.0",
  "description": "blockgame",
  "main": "src/sketch.js",
  "scripts": {
    "start": "concurrently \"nodemon src/sketch.js\"",
    "server": "http-server -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sf",
  "license": "ISC",
  "dependencies": {
    "p5": "^0.5.16"
  }
}
